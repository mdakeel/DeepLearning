# Deep Learning Practice 🚀

This repository contains my experiments, practice notebooks, and implementations of deep learning models using **PyTorch**.  
It serves as a personal learning space where I build, train, and evaluate models on different datasets.

---

## 📂 Repository Structure

- `ANN/MLP/` → Basic Artificial Neural Network & Multi-Layer Perceptron implementations
- `Digit_Classification/` → MNIST/Fashion-MNIST digit classification models
- `Pytorch_Training_Pipeline/` → Custom dataset class, dataloaders, training loops, and evaluation scripts
- More models and experiments will be added as I progress.

---

## ⚙️ Setup

Clone the repo and install dependencies:

```bash
git clone https://github.com/mdakeel/DeepLearning.git
cd DeepLearning
pip install -r requirements.txt
```
